// DirectoryView — grid of folder + page cards for a wiki or subfolder
// Panels do NOT exist here. This is a full-page directory view.

import React from 'react'

// TODO Phase 2: receive current path as prop, load contents from Zustand store
const DirectoryView: React.FC = () => {
  return (
    <div className="directory-view">
      <p>Directory — folder/page card grid goes here (Phase 2)</p>
    </div>
  )
}

export default DirectoryView
